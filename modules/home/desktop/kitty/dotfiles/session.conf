# Kitty startup session — referenced via `startup_session` in home.nix.
# Every new kitty/OS-window runs fastfetch once, then hands the tab to an
# interactive login shell (so fastfetch greets you on launch but doesn't rerun
# on every command).
launch sh -c "fastfetch; exec ${SHELL:-bash} -l"
